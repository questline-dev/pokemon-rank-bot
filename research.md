# 조회 누락 전수 점검 (2026-09-24)

봇 코드(`포켓몬랭크봇.js`, `포켓몬랭크봇.html`)는 고치지 않은 리서치 기록이다.
검사 스크립트는 `research/` 폴더에 있고, 받은 데이터와 결과물은 커밋하지 않는다(`research/.gitignore`).

---

## 1. 조사 범위와 방법

### 데이터

유지보수노트.md 표에 있는 외부 데이터 5개를 실제로 받아서 썼다.

- 받은 시각: **2026-09-24 08:53 KST** (2026-09-23 23:53 UTC). 같은 날 두 번 받았고 sha256이 같았다.
- gamemaster.json 안의 `timestamp`: `2026-09-23 03:24:34`
- 원본 저장소의 커밋 번호는 이 환경에서 GitHub API 접근이 막혀서 못 남겼다. 대신 sha256을 남긴다.

| 파일 | 크기(byte) | sha256 앞 16자리 |
|------|-----------:|------------------|
| pokemon_species_names.csv | 404,502 | `820cde17074cdb1c` |
| pokemon_species.csv | 56,884 | `e66e2eeb25fd3836` |
| pokemon.csv | 47,082 | `16c81c33188b0eac` |
| pokemon_forms.csv | 63,721 | `99bf8f7ad4dc1f2e` |
| gamemaster.json | 1,691,128 | `946db90132eb2915` |

### 봇 함수를 그대로 부르는 방법

- `research/load-bot.js`가 `포켓몬랭크봇.html`의 `<script>`를 **한 글자도 바꾸지 않고** Node `vm`에서 실행한다.
- 흉내 낸 건 브라우저 바깥 환경뿐이다.
  - `document.getElementById`: 입력창·결과창 등 7개 요소를 값만 담는 가짜 객체로
  - `navigator`, `window`: 빈 객체 (서비스워커 등록은 건너뜀)
  - `fetch`: 같은 URL 요청에 인터넷 대신 `research/data/`에 받아둔 파일을 돌려줌 (내용은 위에서 받은 실제 데이터 그대로)
- 검사에서 직접 부른 봇 함수: `findEnglishName`, `findEnglishNameBySpeciesId`, `getFamilyStats`(안에서 `findFamilyMembers`·`collectReachableFromTarget` 등을 그대로 호출), `runCalc`(화면 출력까지 끝까지), `extractPokemonArrayEntries`, `findSpeciesEntry`, `calcCP`, `levelToIndex`, `normalizeKoreanName`. 원인 확인용 대조 실험(`research/mechanism.js`)에서는 `findFamilyMembers`, `collectReachableFromTarget`, `findChildrenByParent`를 직접 불렀다.

**그대로 부를 수 없었던 것**

- `포켓몬랭크봇.js`(카톡 봇)는 Rhino 전용(`org.jsoup.Jsoup`, 메신저봇R의 `response(... replier ...)`)이라 Node에서 실행할 수 없다.
- 대신 `research/compare-js-html.js`로 두 파일의 같은 이름 함수를 비교했다. 주석·공백·`async/await`를 빼고, 데이터 받는 표기(`fetchRawText(URL)` ↔ `getXxx()`)를 맞춘 뒤 비교했다.
  - 계산·파싱 함수 **33개가 본문까지 똑같다**. `getFamilyStats`, `collectReachableFromTarget`, `findFamilyMembers`, `findEnglishName`도 여기에 들어간다.
  - 다른 건 3개뿐이다: `fetchRawText`(Jsoup ↔ fetch), `fmtRankPart`(🔴 ↔ 빨간 글씨), `debugFindEnglishName`(변수 이름 `e` ↔ `e2`).
  - 카톡 `response()`와 HTML `runCalc()`는 구조가 달라서 눈으로 비교했다. 이름 찾기 → 암수 처리 → `getFamilyStats` → 줄 만들기 순서가 같다.
  - 그래서 아래 결과는 카톡 봇에도 그대로 해당한다고 본다. 다만 **폰에 실제로 들어 있는 봇 버전은 확인하지 못했다.**

### 정답(기대 결과) 기준 — 봇 로직과 따로 만듦

- **진화 방향**: PokeAPI `pokemon_species.csv`의 `evolves_from_species_id`로 조회한 종의 **자손 도감번호**를 모두 구한다. 이전 단계(조상)는 넣지 않는다. 이미 지나간 단계는 안 나오는 게 정상이기 때문이다(유지보수노트.md "핵심 설계").
- **폼 목록**: gamemaster.json에서 그 도감번호들을 쓰는 항목을 전부 넣는다. 메가·프라이멀·지역폼·사이즈·암수 폼이 여기에 들어간다.
- **제외**: 그림자(`_shadow` 또는 tags `shadow`), tags에 `duplicate`가 든 항목. 봇이 일부러 빼는 것이라 누락으로 치지 않았다.
- **차이가 났을 때 분류**
  - PokeAPI는 폼별 진화를 구분하지 못한다. 그래서 차이가 나면 gamemaster의 연결을 따로 따라가 본다(`family.evolutions`, 자식의 `family.parent`, 도달한 도감번호의 메가 태그).
  - 연결이 있는데 빠졌으면 **코드 문제**로 본다.
  - 데이터 자체가 틀렸으면 **원본 데이터 문제**로 본다. 오타, 방향 반대, 실제 게임에 없는 진화가 여기에 들어간다.
  - 판단할 근거가 없으면 **불확실**로 둔다.
- `released: false` 항목은 **미출시(우선순위 낮음)**로 따로 표시했다.
- 유지보수노트.md "알려진 한계"와 "핵심 설계"에 이미 적힌 건 **기존에 알려짐**으로 표시했다.

### IV·CP 기준

- **IV 15/15/15**, CP는 **봇이 고른 조회 대상 종(target)의 레벨 20 CP**다. 봇의 `calcCP`로 계산했다.
  - 예: 흉내내 626, 라이츄 1247
- 결과 목록이 이 기준에 따라 달라지는지도 확인했다. **IV 0/0/0, 레벨 40 CP**로 한 번 더 돌렸고, **1025종 모두 목록이 똑같았다.**
  - 목록은 진화 연결로만 정해지고, CP는 각 줄의 레벨·(X) 판정에만 쓰인다.
- 예외는 `니드런`처럼 암수 없이 친 경우다. CP로 암수를 고르기 때문에 CP 486 / 398 / 9999 세 경우를 따로 검사했다.

### 스크립트 실행 방법

Node 18 이상이 필요하다(검사는 v22.22.2).

```
sh research/fetch-data.sh        # 외부 데이터 5개를 research/data/ 에 받고 FETCHED.txt 에 시각·sha256 기록
node research/audit.js           # 1~5번 검사 (약 4분) → research/out/audit.json
node research/report.js          # 이 문서 "결과 요약"의 숫자와 원본 데이터 불일치 목록 출력
node research/mechanism.js       # 이상 사례 원인 대조 실험 (데이터 복사본만 바꿔서 봇 함수에 넣어봄)
node research/compare-js-html.js # .js / .html 함수 본문 비교
```

---

## 2. 결과 요약

PokeAPI 한국어 이름 **1025종 전부**를 검사했다.

| 검사 | 결과 |
|------|------|
| 1. 이름 조회 | **이상 0종** / 1025. 암수·특수문자 입력 7개(`니드런암`·`니드런암컷`·`니드런수`·`니드런수컷`·`폴리곤z`·`타입널`·`폴리곤2`) 모두 정상. 정규화 후 겹치는 이름 0개. `니드런`(암수 없이)도 문서대로 동작 |
| 2. 종족값 조회 | **이상 0종** / 1025. "종족값 정보를 찾지 못했습니다" 0건, 화면 출력 오류 0건. 41종은 영어 이름으로 바로 못 찾고 도감번호로 찾는 길을 탔지만 전부 맞는 도감번호를 찾음 |
| 3. 진화 계열 | **기대 결과와 다른 종 8종** (아래 표). 그중 출시된 포켓몬이 실제로 틀리게 나오는 건 **4종**: 흉내내·라이츄·차데스·그우린차 |
| 4. 한 이름에 계열 둘 이상 | 해당 도감번호 4개(피카츄·파오리·우파·침바루), 폼마다 진화 대상이 다른 도감번호 20개. **모두 정상** — 새로 빠지는 계열 없음 |
| 5. 암수 폼 4종 | 냐오닉스·에써르·대쓰여너·퍼퓨돈 **모두 두 폼 다 나옴**. 진화 전 단계(냐스퍼·배쓰나이·맛보돈)로 조회해도 정상 |

**3번 진화 계열 — 유형별 개수**

| 유형 | 원인 | 종 수 | 해당 포켓몬 | 새로/기존 | 미출시 |
|------|------|------:|-------------|-----------|--------|
| A. 진화형이 빠짐 | 코드 | 1 | 흉내내(마임꽁꽁 빠짐) | 새로 발견 | 아님 |
| B. 이전 단계·진화 관계 없는 포켓몬이 더 나옴 | 코드 | 2 | 라이츄(피카츄 6줄), 아이스크(껍질몬) | 새로 발견 | 껍질몬만 미출시 |
| C. 진화 방향이 거꾸로 | 원본 데이터 | 2 | 차데스(그우린차 빠짐), 그우린차(차데스가 더 나옴) | 새로 발견 | 아님 |
| D. 실제로 없는 진화 연결 | 원본 데이터 | 1 | 피오네(마나피가 더 나옴) | 새로 발견 | 둘 다 미출시 |
| E. 특수폼 진화 여부 | 불확실 | 1 | 피츄(특수 피카츄 3개 빠짐) | 새로 발견 | 아님 |
| F. 폼 연결이 데이터에 없음 | 원본 데이터 | 1 | 타입:널(실버디 타입폼 17개 빠짐) | 기존(실바리 항목과 관련) | 전부 미출시 |

- **기존에 알려진 것 중 지금은 재현 안 되는 것**: 암멍이(유지보수노트 표기 "이니마") → 루가루암 황혼폼. 유지보수노트 3번은 PvPoke 오타 때문에 빠질 수 있다고 적었지만, 지금은 결과에 나온다(3-G).
- **문서 표기 차이**: 유지보수노트에 나오는 "에스퍼로"(876 → 에써르), "실바리"(773 → 실버디), "이니마"(744 → 암멍이)는 PokeAPI에 없는 이름이다. 봇에 그대로 치면 "영어 이름을 찾지 못했습니다"가 나온다. 이번 검사는 괄호 안의 PokeAPI 이름으로 했다.

---

## 3. 유형별 상세

아래 코드 위치는 `.html` / `.js` 순서다. 두 파일의 해당 함수 본문은 같다.
참고로 `collectReachableFromTarget`은 다음 순서로 동작한다.

- **BFS**(html 858–877행 / js 868–887행): 조회한 도감번호의 폼들에서 `family.evolutions`를 따라간다.
- **부모→최종진화체 보정**(html 879–889 / js 889–899): 진화 대상이 아니면서 `parent`가 있는 항목에 대해, 부모에서 `evolutions[0]`을 따라 끝까지 간 곳이 이미 도달해 있으면 붙인다. 원래는 메가용이다.
- **같은 도감번호 상속**(html 899–919 / js 909–929): 같은 도감번호를 쓰는 도달한 항목이 있으면 그 레벨을 물려받아 붙인다. 원래는 family 없는 메가·프라이멀용이다.

### A. 흉내내 → 마임꽁꽁(mr_rime)이 빠짐 — 코드, 새로 발견

- 재현: `?포켓몬 흉내내 15 15 15 626`
- 봇 결과:
  ```
  - Mr. Mime (마임맨) / S867[lvl23.5 cp1496] / H22[lvl49 cp2489] / M1[lvl50 cp2518] / L3948(X)
  - Mr. Mime (Galarian) (마임맨) / S2587 / H1[lvl50 cp2338] / M1[lvl50 cp2338] / L699(X)
  - Mime (Jr) (흉내내) / S1[lvl50 cp1238] / H1[lvl50 cp1238] / M1[lvl50 cp1238] / L698(X)
  ```
- 기대 결과: 흉내내, 마임맨, 가라르 마임맨, **마임꽁꽁**
  - PokeAPI 기준 자손은 122(마임맨)와 866(마임꽁꽁)이다.
  - gamemaster에서도 `mr_mime_galarian.parent = mime_jr`, `mr_rime.parent = mr_mime_galarian`으로 이어져 있다.
  - 마임맨으로 조회하면 마임꽁꽁이 정상으로 나온다.
- 원인: `collectReachableFromTarget`
  - `mime_jr.evolutions`에는 `mr_mime`만 있다. 그래서 가라르 마임맨은 BFS가 아니라 뒤쪽 **부모→최종진화체 보정**으로 붙는다.
  - 이 보정은 새로 붙인 항목의 `evolutions`를 다시 따라가지 않는다. 그래서 그 진화형 마임꽁꽁은 붙지 않는다.
  - 마임꽁꽁은 봇이 모은 family 목록에는 있다. 하지만 "진화 대상"이라 보정에서 빠지고, 도감번호도 달라 상속도 안 된다.
  - 대조 실험 [2]: 데이터 복사본의 `mime_jr.evolutions`에 `mr_mime_galarian`을 넣으면 마임꽁꽁이 나온다.
- 참고: 가라르 마임맨이 이미 결과에 나오는데 그 진화형만 빠지는 **내부 불일치**다. 실제 포켓몬GO에서 흉내내가 가라르 마임맨으로 진화하는지는 확인 못 했다(5장 참고).

### B. 이전 단계·진화 관계 없는 포켓몬이 더 나옴 — 코드, 새로 발견

#### B-1. 라이츄 → 피카츄 6줄이 나옴 (출시)

- 재현: `?포켓몬 라이츄 15 15 15 1247`
- 봇 결과(10줄 중 앞 6줄이 문제):
  ```
  - Pikachu (피카츄) / S1[lvl50 cp1060] / H1[lvl50 cp1060] / M1[lvl50 cp1060] / L1705(X)
  - Pikachu (Horizons) (피카츄) / ...
  - Pikachu (Kariyushi) (피카츄) / ...
  - Pikachu (Pop Star) (피카츄) / ...
  - Pikachu (Rock Star) (피카츄) / ...
  - Pikachu (Shaymin Scarf) (피카츄) / ...
  - Raichu (라이츄) / S742[lvl24 cp1496] / H1[lvl50 cp2467] / M1[lvl50 cp2467] / L1209(X)
  - Mega Raichu X (라이츄) / ...
  - Mega Raichu Y (라이츄) / ...
  - Raichu (Alolan) (라이츄) / ...
  ```
- 기대 결과: 라이츄, 메가 라이츄 X/Y, 알로라 라이츄. 피카츄는 이전 단계라 나오면 안 된다.
- 원인: `collectReachableFromTarget`의 **부모→최종진화체 보정**과 **같은 도감번호 상속**이 이어서 걸린다.
  - 코스튬 피카츄 5종(horizons·kariyushi·pop_star·rock_star·shaymin)은 데이터상 `parent = pichu`인데, `pichu.evolutions`에는 없다.
  - 그래서 보정이 이들을 메가처럼 취급한다. pichu → pikachu → raichu로 따라간 끝(raichu)이 도달해 있으니 붙인다.
  - 이어서 **같은 도감번호 상속**이 도감 25인 일반 피카츄까지 붙인다.
  - 대조 실험 [1]: 코스튬 5종을 빼면 피카츄가 사라진다. 1개만 남겨도 일반 피카츄가 따라 나온다.
- 영향: 피카츄 줄의 (X) 판정에는 라이츄의 레벨이 쓰인다.

#### B-2. 아이스크 → 껍질몬(shedinja)이 나옴 (껍질몬은 PvPoke 기준 미출시 → 우선순위 낮음)

- 재현: `?포켓몬 아이스크 15 15 15 1125`
- 봇 결과:
  ```
  - Ninjask (아이스크) / S1229[lvl26.5 cp1491] / H1[lvl50 cp2226] / M1[lvl50 cp2226] / L3408(X)
  - Shedinja (껍질몬) / S1[lvl50 cp445] / H1[lvl50 cp445] / M1[lvl50 cp445] / L1[lvl50 cp445]
  ```
- 기대 결과: 아이스크만. 껍질몬은 토중몬에서 갈라지는 진화라 아이스크에서는 갈 수 없다. 토중몬으로 조회하면 셋 다 나오는 게 정상이고, 실제로 그렇게 나온다.
- 원인: B-1과 같은 **부모→최종진화체 보정**이다.
  - `shedinja.parent = nincada`인데 `nincada.evolutions`에는 `ninjask`만 있다.
  - 그래서 nincada → ninjask로 따라간 끝(아이스크)에 껍질몬이 붙는다.
  - 대조 실험 [3]: `nincada.evolutions`에 shedinja를 넣으면 사라진다.

### C. 차데스/그우린차 — PvPoke 데이터의 진화 방향이 거꾸로 (원본 데이터, 새로 발견)

- 재현: `?포켓몬 차데스 15 15 15 650`, `?포켓몬 그우린차 15 15 15 1690`
- 봇 결과:
  ```
  ● 차데스 15 15 15 650 : lvl20
  - Poltchageist (차데스) / S1[lvl50 cp1287] / H1[lvl50 cp1287] / M1[lvl50 cp1287] / L2958(X)

  ● 그우린차 15 15 15 1690 : lvl20
  - Poltchageist (차데스) / S1[lvl50 cp1287] / H1[lvl50 cp1287] / M1[lvl50 cp1287] / L2958(X)
  - Sinistcha (그우린차) / S2045(X) / H1272[lvl29.5 cp2493] / M1[lvl50 cp3343] / L1481(X)
  ```
- 기대 결과
  - 차데스: 차데스, **그우린차**
  - 그우린차: 그우린차만. 차데스는 이전 단계다.
- 원인: gamemaster가 `poltchageist.family = {parent: "sinistcha"}`, `sinistcha.family = {evolutions: ["poltchageist"]}`로 **방향이 반대**다. PokeAPI는 1013(그우린차)이 1012(차데스)에서 진화한다고 되어 있다.
  - 코드는 데이터를 그대로 따른다.
  - 대조 실험 [4]: 데이터 복사본의 방향만 바로잡으면 두 조회 모두 기대 결과대로 나온다.

### D. 피오네 → 마나피가 진화형으로 나옴 — 원본 데이터, 새로 발견, 둘 다 미출시(우선순위 낮음)

- 재현: `?포켓몬 피오네 15 15 15 1203`
- 봇 결과: `Phione (피오네)`, `Manaphy (마나피)` 두 줄
- 기대 결과: 피오네만. PokeAPI상 둘 사이에 진화 관계가 없다.
- 원인: gamemaster에 `manaphy.family.parent = "phione"`이 들어 있다.
  - 피오네는 family 정보가 없어서 `getFamilyStats`의 역방향 보정(`findChildrenByParent`, html 761–783 / js 771–793)을 탄다. 이 보정이 마나피를 자식으로 찾아 붙인다.
  - 대조 실험 [5] 참고.

### E. 피츄 → 특수 피카츄 일부만 나옴 — 불확실, 새로 발견

- 재현: `?포켓몬 피츄 15 15 15 270`
- 봇 결과: 피카츄·코스튬 피카츄 5종(Horizons·Kariyushi·Pop Star·Rock Star·Shaymin Scarf)·라이츄·메가 라이츄 X/Y·알로라 라이츄·피츄 (11줄)
- 기대 결과(PokeAPI 기준): 도감 25의 모든 폼이다. `pikachu_5th_anniversary`, `pikachu_flying`, `pikachu_libre` 3개가 빠졌다.
- 판단
  - PokeAPI는 폼을 구분하지 못한다. gamemaster 기준으로는 빠진 3개가 family 정보가 없어 피츄와 연결이 없다. 나온 5개는 `parent = pichu`로 연결돼 있다.
  - 실제 게임에서 피츄가 이 특수 피카츄들로 진화할 수 있는지 데이터로는 판단할 수 없어서 **불확실**로 둔다.
  - 코스튬 5개는 나오고 3개는 안 나오는 불일치는 데이터 표현 차이 때문이다.

### F. 타입:널 → 실버디 타입폼 17개가 빠짐 — 원본 데이터(연결 없음), 전부 미출시(우선순위 낮음), 기존 항목과 관련

- 재현: `?포켓몬 타입:널 15 15 15 1522`
- 봇 결과: `Type (Null) (타입:널)`, `Silvally (실버디)` 두 줄
- 기대 결과: 도감 773의 폼 전부다. `silvally_bug`~`silvally_water` 17개가 빠졌다.
- 원인: 타입폼 17개는 family 정보가 없어서 어디에도 연결되지 않는다. 같은 도감번호 형제를 펼치는 보정(html 729–754 / js 739–764)은 조회한 종의 도감번호(772)에만 적용된다.
- 기존 여부: 유지보수노트 "알려진 한계"의 실바리 항목은 "정식 출시되면 보정 패스가 잡아줄 것"이라고 예상했다. 실제로 기본 실버디는 잡히지만 타입폼은 안 잡힌다.

### G. (참고) 기존에 알려진 것 — 지금은 재현 안 됨: 암멍이 → 루가루암 황혼폼

- 재현: `?포켓몬 암멍이 15 15 15 543` (rockruff의 PokeAPI 한국어 이름은 "암멍이"다. 유지보수노트의 "이니마"로 치면 이름을 못 찾는다)
- 봇 결과: 암멍이, 루가루암 황혼폼·한낮폼·한밤중폼 모두 나온다.
- 유지보수노트 3번에는 `rockruff.evolutions`의 오타(`lycranroc_dusk`) 때문에 "이니마"(암멍이) 조회 시 황혼폼이 빠질 수 있다고 적혀 있다. 오타는 지금도 데이터에 그대로 있다.
- 그런데 뒤쪽 보정 두 개(부모→최종진화체 보정, 같은 도감번호 상속)가 **각각 따로** 황혼폼을 붙여준다. 레벨은 암멍이 레벨을 물려받아서 값도 맞다.
- 대조 실험 [6]: parent와 도감번호를 둘 다 바꿔야 사라진다.
- 결론: 유지보수노트 문구가 현재 동작과 다르다.

### 4번 검사 상세 — 한 이름에 진화 계열이 둘 이상인 경우

- 한 도감번호에 `family.id`가 둘 이상이거나 family 있음/없음이 섞인 곳: 4곳, 모두 정상.
  - 피카츄(25): FAMILY_PIKACHU 6개 + family 없음 3개
  - 파오리(83): 일반은 family 없음, 가라르는 FAMILY_FARFETCHD → 창파나이트까지 나옴
  - 우파(194): FAMILY_WOOPER / FAMILY_WOOPER_PALDEAN → 누오·토오 둘 다 나옴
  - 침바루(211): 일반은 family 없음, 히스이는 FAMILY_QWILFISH → 장침바루까지 나옴
- 폼마다 진화 대상(`family.evolutions`)이 다른 도감번호 20개도 **모두 정상**이다(`node research/report.js`의 [4] 뒷부분). 꼬렛·모래두지·식스테일·디그다·나옹·가디·꼬마돌·데구리·포니타·야돈·질퍽이·찌리리공·우파·포푸니·지그제구리·도롱충이·달막화·데스마스·조로아·호바귀가 여기에 들어간다.

### 5번 검사 상세 — 암수 폼 4종

| 조회 | 결과에 나온 폼 | 비고 |
|------|----------------|------|
| `?포켓몬 냐오닉스 15 15 15 1213` | Meowstic (Female), Meowstic (Male) | 두 폼 종족값이 같아 등수도 같음 |
| `?포켓몬 냐스퍼 15 15 15 719` | 냐스퍼 + 냐오닉스 두 폼 | |
| `?포켓몬 에써르 15 15 15 1396` | Indeedee (Female), Indeedee (Male) | 폼마다 자기 레벨로 따로 계산됨 |
| `?포켓몬 대쓰여너 15 15 15 1731` | Basculegion (Female), Basculegion (Male) | PvPoke 기준 두 폼 다 미출시 |
| `?포켓몬 배쓰나이 15 15 15 1194` | 배쓰나이 + 대쓰여너 두 폼 | |
| `?포켓몬 퍼퓨돈 15 15 15 1490` | Oinkologne, Oinkologne (Female) | 수컷 줄 이름엔 "(Male)"이 안 붙음(PvPoke 표기 그대로) |
| `?포켓몬 맛보돈 15 15 15 418` | 맛보돈 + 퍼퓨돈 두 폼 | |

데이터에는 `espurr.evolutions = ["meowstic","meowstic"]`처럼 암컷이 빠지고 수컷이 두 번 들어간 곳이 있다(4장 원본 데이터 문제 목록). 그래도 뒤쪽 보정이 암컷 폼을 붙여줘서 결과는 정상이다.

---

## 4. 원본 데이터 문제 목록

코드가 아니라 PvPoke gamemaster.json 쪽 문제다. `node research/report.js` 끝부분에 전체 목록이 나온다.

| 데이터 | 내용 | 봇 결과에 미치는 영향 | 새로/기존 |
|--------|------|----------------------|-----------|
| `poltchageist` / `sinistcha` | 진화 방향이 반대 (차데스의 parent가 그우린차) | 차데스·그우린차 결과가 틀림 (3-C) | 새로 |
| `manaphy.family.parent = "phione"` | 실제로 없는 진화 | 피오네 결과에 마나피가 나옴, 미출시 (3-D) | 새로 |
| `nincada.evolutions = ["ninjask"]` | `shedinja.parent = nincada`인데 evolutions에 껍질몬이 없음 | 코드 보정과 겹쳐 아이스크에 껍질몬이 나옴 (3-B-2) | 새로 |
| `mime_jr.evolutions = ["mr_mime"]` | `mr_mime_galarian.parent = mime_jr`인데 evolutions에 없음 | 코드 문제와 겹쳐 흉내내에서 마임꽁꽁이 빠짐 (3-A) | 새로 |
| 코스튬 피카츄 | 5종은 `parent = pichu`인데 `pichu.evolutions`에 없음, 3종은 family 없음 | 라이츄에 피카츄가 나옴(3-B-1), 피츄 불확실(3-E) | 새로 |
| `silvally`, `silvally_*` 17개 | family 없음 | 타입:널에서 타입폼이 빠짐, 미출시 (3-F) | 기존 항목과 관련 |
| `rockruff.evolutions`의 `"lycranroc_dusk"` | 오타 (`lycanroc_dusk`) | **지금은 영향 없음**, 보정이 살려줌 (3-G) | 기존 |
| `carkol.family.parent = "boltund"` | 탄동(rolycoly)이어야 함 | 영향 없음 (evolutions로 정상 탐색) | 새로 |
| `coalossal.family.parent = "rolycoly"` | 탄차곤(carkol)이어야 함 | 영향 없음 | 새로 |
| `raticate.family.parent = "rattata_alolan"` | 일반 꼬렛(rattata)이어야 함 | 영향 없음 | 새로 |
| `espurr` / `lechonk` / `tandemaus`의 evolutions | 같은 이름이 두 번 들어 있고 암컷·다른 폼이 빠짐 (`["meowstic","meowstic"]` 등) | 영향 없음, 보정이 살려줌 (tandemaus 쪽은 미출시) | 새로 |
| `scyther_shadow` 등 그림자 3개 | evolutions가 없는 항목(`kleavor_shadow`, `farigiraf_shadow`, `wyrdeer_shadow`)을 가리킴 | 영향 없음 (봇이 그림자를 뺌) | 새로 |

- `mothim.parent = burmy_trash`(도롱충이 세 폼 중 하나만 부모로 표시)와 `toxtricity`(접미사 없는 항목이 `toxel.evolutions`에 없음)도 불일치다. 하지만 결과에는 영향이 없었다.
- 알로라 라이츄·히스이 폼처럼 "원종이 부모"로 적힌 지역폼은 실제 게임과 맞는 정상 패턴이다. 문제로 치지 않았다.

---

## 5. 확인 못 한 것과 불확실한 것

- **실제 게임 규칙은 확인 못 함.** 판단은 PokeAPI·PvPoke 데이터 기준이다.
  - 피츄 → 특수 피카츄(3-E), 흉내내 → 가라르 마임맨(3-A)이 포켓몬GO에서 실제로 가능한지 확인하지 못했다.
  - `released` 값도 PvPoke 표시 그대로 썼다. 껍질몬·대쓰여너처럼 미출시로 표시된 포켓몬이 실제 게임과 다를 수 있다. PvPoke 기준 전부 미출시인 종은 74종이다.
- **레벨·CP·등수 값이 맞는지는 이번 범위 밖**이다. 결과 목록에 어떤 포켓몬이 들어가는지만 봤다.
  - 지가르데 퍼펙트폼, 네크로즈마 울트라, 에터나투스 이터나맥스(알려진 한계)는 목록에는 들어가지만, 그 줄의 값은 다시 검증하지 않았다.
  - 모습이 바뀌는 폼(킬가르도 블레이드/실드, 모르페코, 윽우지, 따라큐, 불비달마 달마모드)도 목록만 봤다.
- **카톡 봇은 직접 실행하지 못했다.** 함수 본문 비교로 대신했다(1장). 폰의 메신저봇R에 들어 있는 버전이 저장소 최신과 같은지도 확인하지 못했다.
- **데이터는 2026-09-24 08:53 KST 시점 스냅샷이다.** PvPoke·PokeAPI가 바뀌면 결과가 달라질 수 있다. 원본 커밋 번호 대신 sha256을 남겼다.
- **정답 기준의 한계**
  - 조회한 도감번호의 폼은 전부 보여주는 게 설계다(사이즈·폼 원칙). 그래서 "같은 도감번호의 폼이 전부 맞는 후보인지"(예: 피카츄 조회 시 특수폼 포함 9줄 전부)는 판단하지 않았다.
  - PokeAPI상 진화 관계가 없는데 gamemaster에만 연결된 경우는 "더 나옴"으로만 잡힌다.
- **입력 형식**: `알로라나옹`, `가라르포니타`, `팔데아우파`, `메가리자몽`처럼 지역·메가 이름을 붙이면 "영어 이름을 찾지 못했습니다"가 나온다. 기본 이름으로 조회하면 모든 폼이 나오는 설계라 누락으로 치지 않았다. 다만 사용자가 이렇게 칠 수는 있다.
- `?포켓몬디버그`(진단 명령)는 검사하지 않았다.

---

## 6. 다음 단계 제안 (우선순위만)

고치는 방법은 아직 정하지 않았다. 무엇부터 볼지만 적는다.

1. **라이츄(3-B-1)와 흉내내(3-A)** — 출시된 포켓몬이 코드 때문에 틀리게 나온다.
   - 라이츄는 많이 조회하는 포켓몬이라 눈에 잘 띈다.
   - 둘 다 `collectReachableFromTarget`의 BFS 뒤 보정에서 생기니 같이 보는 게 좋다.
   - 고친 뒤에는 `node research/audit.js`를 다시 돌려서, 그동안 고친 사례(우파·메가 31종·트리토돈·산호르곤·니드런)가 그대로인지 확인한다.
2. **차데스/그우린차(3-C)** — 출시된 포켓몬인데 원본 데이터 문제다. PvPoke가 고치기를 기다릴지, 우리 쪽에서 따로 대응할지 먼저 정해야 한다.
3. **피츄(3-E)** — 실제 게임 규칙을 사용자가 판단해줘야 한다.
4. **미출시 관련(우선순위 낮음)**: 아이스크 → 껍질몬(3-B-2, 1번과 원인이 같아서 같이 해결될 가능성이 있음), 피오네 → 마나피(3-D), 타입:널 → 실버디 타입폼(3-F).
5. **문서 정리**: 유지보수노트.md를 고칠 때 아래를 반영한다.
   - 암멍이 황혼폼 문구(3-G)
   - "에스퍼로" → "에써르", "실바리" → "실버디", "이니마" → "암멍이" 표기
   - 암수 폼 4종 확인 결과(5번 검사)

# 프롬프트 가이드 (이 프로젝트의 시행착오 회고)

## 실제로 겪은 왔다갔다

1. **"CP가 정확하게 산출되면 좋겠어"** (최초 요청) → 최대레벨을 사이트와 다르게 상한 없이 탐색해서 어긋나던 것, IV 입력 순서(공격/체력/방어 → 공격/방어/체력)가 표준과 달랐던 것을 발견하고 고침.
2. **"영어이름 옆에 한글이름"** + **"HTML에 지우기 버튼"** → 간단한 기능 추가였는데, 구현 중 gamemaster.json의 `dex`(도감번호)가 어떤 폼이든 원종과 동일하다는 걸 이용해서 한글이름 CSV와 매칭시킴.
3. **"호바귀(펌킨인)를 조회하면 에러가 남"** → 원인을 파다 보니 PvPoke gamemaster.json에 "기본형" 단독 항목이 아예 없는 포켓몬이 30종 가까이 있다는 걸 발견. 여기서부터 이 프로젝트가 예상보다 훨씬 깊어짐.
4. **"펌킨인은 최종진화체니까 이전 개체(호바귀)로 못 감. 슈퍼/하이퍼리그도 참전 가능해야 함"** → 진화 방향 필터링(`collectReachableFromTarget`)을 새로 설계. 메가진화의 `family.parent`가 최종진화체를 건너뛰는 PvPoke 데이터 특성 때문에 단순 정방향 그래프 탐색으로는 메가가 빠지는 문제를 별도로 보정해야 했음.
5. **사용자가 실제 게임 화면 스크린샷을 보내서 "Super의 경우 1497이 나온다"고 지적** → 코드로는 안 보이던, "사이즈마다 실제 레벨을 따로 구해야 하는데 하나로 퉁쳤다"는 버그를 발견. **스크린샷 같은 실측 증거가 코드 리뷰보다 먼저 버그를 잡아낸 사례.**
6. 이후 독립 리뷰(adversarial review) 5라운드를 거치며 같은 유형의 버그(dex는 같은데 독립 개체로 잘못 취급됨)가 메가 → 프라이멀 → (지가르데 등 일부는 보류) 순으로 점점 더 깊게 발견됨. 매 라운드 실제 라이브 데이터(gamemaster.json/PokeAPI CSV)를 직접 받아서 손으로 검산하며 검증함. 5라운드 만에 APPROVE 받고 문서화까지 마무리.
7. **APPROVE 받은 지 얼마 안 돼서 사용자가 "물짱이 조회했는데 메가가 안 나오네"라고 신고** → 알고 보니 메가진화 62종 중 절반(31종, 대짱이 포함)이 5라운드에서 고친 것과 *데이터 구조 자체가 다른* 패턴(`family` 필드가 아예 없음)이었음. 5라운드 리뷰가 "전수조사"라고 했지만 실제로는 메가/프라이멀 태그 62개 중 일부 형태만 검증했던 것이다. **"전수조사 했다"는 리뷰 결과도 100% 믿지 말고, 사용자가 실제로 써보다 발견한 반례가 나오면 그게 진짜 신호**라는 교훈. 이걸 고치는 과정에서 뮤츠/아머드뮤츠처럼 같은 도감번호에 독립된 두 개체가 있는 케이스까지 추가로 발견돼서 2라운드(6, 7)가 더 진행됨.

8. **"니드런 1 11 11 486 이걸 못찾는 문제를 좀 찾아봐"** + **"암수 구분때문일 것 같은데"** (2026-09-24) → 사용자 추측이 정확했음. PokeAPI 이름이 `니드런♀`/`니드런♂`라서 `니드런`과 정확히 일치하는 이름이 없었다. 정확한 입력값을 같이 줘서 바로 재현하고, CP로 암컷(레벨 25)이라는 것까지 계산으로 확인함. 같은 유형(한글 말고 다른 문자가 든 이름)을 전수조사해서 `폴리곤Z`(소문자로 치면 실패), `타입:널`(콜론 빼면 실패)까지 같이 고침. 고치는 방식은 선택지(자동 판별 vs 항상 되묻기)와 봇 안내 문구 초안을 먼저 보여주고 확인받은 뒤 구현함.

9. **"공개 저장소에 개인 아이디가 드러나 있어"** (2026-09-24) → 9/14에 조직으로 옮기면서 noreply 주소를 쓰면 가려진다고 생각했다. 그런데 noreply 주소가 `숫자ID+아이디` 형식이라 아이디가 그대로 보였다. 문서 한 곳만 고치면 옛 커밋에 계속 남아서, 커밋 10개를 전부 다시 쓰고 독립 검토 3번을 거쳐 강제 푸시했다. 교훈: **'가렸다'고 생각한 값도 공개 API로 거꾸로 찾아지는지 한 번 확인할 것.**

## 다음에 비슷한 걸 요청할 때 쓸 수 있는 프롬프트 틀

**버그를 발견했을 때** ("~하면 이상하게 나와" 보다 이렇게):
> "`<정확한 입력값>`으로 조회하면 `<실제 나온 결과>`가 나오는데, `<왜 이상한지 이유>`. (가능하면) 실제 게임/레퍼런스 사이트에서는 `<정답이라고 생각하는 값>`이 나와야 할 것 같아."

→ 스크린샷이 있으면 훨씬 빠르게 원인을 찾는다(5번 사례 참고). 텍스트로 설명하기 애매한 "화면에 뭐라고 나오는지" 이슈는 스크린샷이 최고.

**기능 추가를 요청할 때**:
> "`<기존 기능>`에 `<추가하고 싶은 것>`을 넣고 싶어. `<왜 필요한지>`."

→ 예: "영어이름 옆에 한글이름을 추가할 수 있을까" 처럼 짧아도 충분함. 애매한 부분(어느 폼까지 한글이름을 붙일지 등)은 알아서 판단해서 구현 후 결과를 보여주고, 그 판단이 틀렸으면 그때 고치는 방식으로 진행됨.

**같은 유형의 문제가 또 있을지 의심될 때**:
> "이거랑 똑같은 이유로 다른 포켓몬도 똑같이 틀리는 거 아니야?"

→ 이 질문 하나로 지가르데/네크로즈마 등 미해결 케이스를 미리 알게 됐음(4-5번 라운드 리뷰에서 실제로 나온 패턴). 비슷한 데이터 구조를 가진 다른 케이스가 있는지 먼저 전수 조사부터 시키면 왔다갔다를 줄일 수 있음.

# 유지보수 노트

## 왜 이렇게 만들었나

카카오톡 봇(메신저봇R, Rhino JS 엔진)과 PC용 HTML 버전이 완전히 같은 계산 로직을 쓴다. 두 파일은 항상 쌍으로 수정해야 하며, HTML 쪽은 네트워크 호출을 `Jsoup`(Java) 대신 브라우저 `fetch()`로, 동기 함수를 `async/await`로 바꾼 것 말고는 로직이 100% 동일해야 한다.

메신저봇R은 Rhino라는 오래된 JS 엔진을 쓰기 때문에 `.js` 파일은 항상 ES5 문법(`var`/`function`, `let`/`const`·화살표 함수·템플릿 리터럴·`for...of` 금지)으로만 작성한다.

## 의존하는 외부 데이터 (전부 raw.githubusercontent.com)

| 용도 | URL |
|------|-----|
| 한국어 이름 → species_id | `PokeAPI/pokeapi` `pokemon_species_names.csv` |
| species_id → 영어 슬러그 | `PokeAPI/pokeapi` `pokemon_species.csv` |
| species_id → 폼(variety) 목록·기본폼 여부 | `PokeAPI/pokeapi` `pokemon.csv` |
| variety → 세부 폼(form)·기본폼 여부 | `PokeAPI/pokeapi` `pokemon_forms.csv` |
| 종족값·진화계열·PvP 데이터 | `pvpoke/pvpoke` `gamemaster.json` |

이 중 아무 저장소가 구조를 바꾸면(컬럼 순서, 필드명 등) 해당 파싱 함수가 조용히 깨질 수 있다. 다시 열었을 때 "종족값 정보를 찾지 못했습니다" 에러가 자주 뜨면 제일 먼저 이 5개 URL이 여전히 살아있고 형식이 같은지 `curl`로 확인할 것.

## 핵심 설계: "실제 조회한 개체가 갈 수 있는 범위"만 보여주기

가장 많이 헤맸던 부분이라 자세히 남긴다.

포켓몬GO는 진화(파워업)를 되돌릴 수 없다. 그래서 이 봇은:

1. 입력한 이름 → 종족값을 찾고, 입력한 CP로 "그 개체의 실제 현재 레벨"을 역산한다.
2. 그 개체가 앞으로 갈 수 있는 진화 단계(메가진화 포함)만 보여주고, 이미 지나친 이전 단계는 안 보여준다.
3. 각 진화 단계로 "지금 이 레벨에" 갔을 때 CP가 리그 상한을 넘으면 그 리그는 `(X)`로 참전 불가 표시.

이걸 구현한 `collectReachableFromTarget` 함수(`.js`/`.html` 양쪽에 동일하게 있음)가 이 프로젝트에서 가장 복잡한 부분이다. 세 가지 까다로운 케이스를 처리한다:

### 1. 사이즈가 여러 개인 포켓몬 (호바귀/펌킨인 등)

Small/Average/Large/Super는 서로 다른 종족값을 가진 완전히 별개의 개체다. PokeAPI에는 이 "사이즈" 개념이 아예 없어서(포켓몬GO 전용 메커니즘) 봇이 실제 어느 사이즈인지 알 방법이 없다. 그래서 같은 도감번호를 쓰는 사이즈 전부를 각자 독립적으로 "실제 레벨"을 계산해서 나란히 보여준다. 사용자가 자기 CP와 정확히 일치하는 줄을 보고 어느 사이즈인지 스스로 판단하면 된다.

**주의**: "실제 레벨"을 사이즈별로 따로 구해야 한다. 예전 버전은 하나의 레벨을 모든 사이즈에 재사용해서, 서로 다른 사이즈인데 같은 레벨 취급을 하는 바람에 (X) 판정이 틀리는 버그가 있었다(스크린샷으로 실제 사례 확인함: Gourgeist Super가 잘못 참전불가 처리됐었음).

### 2. 메가진화/프라이멀리버전 (일시적 변신)

메가/프라이멀은 dex 번호가 원종과 같지만(예: 리자몽=6, 메가리자몽X=6) 독립된 개체가 아니라 "원종이 일시적으로 변신한 상태"다. 원종의 실제 레벨을 그대로 물려받아야지, 따로 CP를 역산하면 안 된다.

- PvPoke gamemaster.json에서 메가/프라이멀은 `tags` 배열에 `"mega"`가 공통으로 붙어있다(프라이멀도 마찬가지! `speciesId`에는 `_mega`가 안 들어있는데도 태그는 같다). 이 태그로 판별한다.
- 또 하나 까다로운 점: 메가 계열 중 **절반**(리자몽 등 62종 중 31종)은 `family.id`가 있긴 한데 `.parent`가 진화 그래프상 진짜 부모(예: 리자몽)가 아니라 그 전 단계(예: 리자드)를 가리킨다. 그래서 단순히 "진화(evolutions) 방향으로만 따라가기"로는 메가를 못 찾는다. → 부모를 따라 리프(최종 진화체)까지 걸어가서, 그 리프가 이미 도달 범위에 있으면 메가도 같이 붙여준다.
- **나머지 절반**(대짱이 메가, 팬텀 메가, 야도란 메가, 한카리아스 메가, 케이오가/그란돈 프라이멀 등 31종)은 `family` 필드 자체가 아예 없다(`.id`도 `.parent`도 없음). 이 경우 `findFamilyMembers`(family.id로 찾는 함수)에 애초에 안 걸려서 결과에서 통째로 빠지는 버그가 있었다(2026-09-14, 사용자가 대짱이 메가가 안 나온다고 신고해서 발견했고 실제 게임 스크린샷과 대조해서 확인함). `getFamilyStats`에서 `findTransientFormsForDexes`로 이런 폼들을 별도로 찾아서 family 배열에 합쳐주고, `collectReachableFromTarget`의 보정 패스가 "같은 dex를 쓰는 이미 도달한 다른 단계"의 레벨을 그대로 물려받게 한다(고정점 반복 - 꼭 seed일 필요 없음, 진화해서 도달한 단계여도 됨).
- 이 "같은 dex 상속" 로직에는 한 가지 더 있다: 같은 dex에 독립된 두 개체가 다 있는 경우(뮤츠 vs 아머드뮤츠, dex 150 둘 다 `family` 없음) 메가/프라이멀이 어느 쪽 레벨을 물려받아야 하는지 애매해진다. 실제 게임에서 메가진화는 코스튬/특수폼이 아니라 항상 "이름에 접미사 없는 기본 개체"에서만 가능하므로(아머드뮤츠는 메가 못 감), speciesId에 `_`가 없는 후보를 우선하도록 처리했다. 라이브 데이터 전수조사 결과 이 케이스는 현재 뮤츠가 유일하다.

### 3. 진화 대상 자체에 family 정보가 없는 포켓몬 (트리토돈 등)

깝질무(Shellos)는 `family.evolutions`로 "트리토돈(gastrodon)으로 진화한다"고 정확히 가리키는데, 정작 트리토돈 자기 자신은 `family` 필드가 아예 없다(메가/프라이멀과 달리 `"mega"` 태그도 없는, 그냥 평범한 최종진화체인데도 그렇다). `findFamilyMembers`가 "family.id가 같은 멤버"만 모으는 방식이라, family.id로 못 찾아지는 트리토돈이 결과에서 통째로 빠지는 버그가 있었다(2026-09-14, 사용자가 깝질무 조회 시 트리토돈이 안 나온다고 신고해서 발견). `findFamilyMembers`에 보정 패스를 추가해서, 이미 모은 멤버들의 `evolutions`가 가리키는 대상을 이름으로 직접 찾아 추가하도록 고쳤다(고정점 반복 - 그렇게 추가된 항목이 또 다른 family-less 대상을 가리킬 수도 있어서).

**주의**: 이 보정은 "이름이 정확히 일치하는 항목을 찾을 수 있을 때"만 동작한다. PvPoke 자체 데이터에 오타가 있는 경우(예: 이니마→라이코룡 황혼폼 진화가 `"lycranroc_dusk"`로 오타나 있어서 실제 `"lycanroc_dusk"`와 안 맞음)는 이 보정으로도 못 고친다. PvPoke 쪽 오타라 우리 코드 문제가 아니다. 라이코룡을 직접 조회하면(황혼폼 자체가 `family.id`를 정상적으로 갖고 있으므로) 3폼 다 나오지만, 이니마(rockruff)를 조회하면 황혼폼만 빠질 수 있다.

**반대 방향도 있다(코산호 → 산호르곤)**: 위는 "부모가 진화형을 정확히 가리키는데 진화형 자신에게 family가 없는" 경우고, 이건 그 반대다 - 코산호(corsola, dex 222)는 `family` 필드가 아예 없어서(일반/갈라르 두 폼 다) 애초에 `findFamilyMembers`(family.id 기반)를 탈 수조차 없고 `collectDexCandidates`(dex 기반) 경로로 빠진다. 근데 그 진화형인 산호르곤(cursola, dex 864)은 `family.parent: "corsola"`로 정상적으로 부모를 가리키고 있어서, dex가 다른 이 둘을 이어줄 연결고리가 원래 코드엔 없었다. `findChildrenByParent`로 "지금 모은 후보들을 부모로 가리키는 항목"을 역방향으로 찾아 보정했다(2026-09-14, 트리토돈 버그를 고치면서 독립 리뷰에서 발견).

### 4. 지역폼끼리 서로 다른(완전히 별개인) 진화계열을 쓰는 경우 (우파/팔데아 우파 등)

우파(Wooper)와 팔데아 우파는 PokeAPI 한글이름이 똑같이 "우파" 하나뿐이다(PokeAPI는 지역폼을 별도 이름으로 안 구분함). gamemaster.json에서는 이 둘이 `family.id`가 서로 완전히 다른(`FAMILY_WOOPER` vs `FAMILY_WOOPER_PALDEAN`) 별개의 정상적인 진화계열이다(둘 다 family 정보 자체는 멀쩡함 - 위 1~3번 항목과는 다른 유형). 한글이름 "우파"는 항상 PokeAPI를 거쳐 영어 슬러그 "wooper"(일반폼)로만 매칭되기 때문에, `getFamilyStats`가 그 하나의 family.id(`FAMILY_WOOPER`, 누오 계열)만 펼치고 다른 계열(`FAMILY_WOOPER_PALDEAN`, 토오 계열)은 통째로 빠지는 버그가 있었다(2026-09-14, 사용자가 팔데아 우파/토오가 안 나온다고 신고 + 레퍼런스 사이트 스크린샷으로 정확한 수치까지 대조해서 확인함). `getFamilyStats`에서 같은 dex를 쓰는 다른 후보 중 "이미 모은 것과 다른 family.id"를 쓰는 형제가 있으면 그 계열 전체를 마저 펼쳐서 합치도록 고쳤다. 사이즈/폼 케이스와 동일한 원칙("애매하면 다 보여주고 사용자가 CP로 고른다")이 자연스럽게 적용돼서, 일반 우파와 팔데아 우파 둘 다(각자 독립적으로 레벨 계산) 나온다.

### 5. 진화계열 없이 폼만 여러 개인 포켓몬 (자시안/자마젠타/미니오르 등)

`family.id`가 아예 없는 단일 개체인데, 폼(각성/각성해제, 코어/유성 등)이 여러 개인 경우. 어느 폼이 진짜 "기본"인지 PvPoke 자체 이름 순서로는 알 수 없어서(예전 버그: 자시안을 물어보면 조용히 "크라운(각성)" 폼으로 잘못 계산됨), PokeAPI의 `pokemon.csv`/`pokemon_forms.csv`에 있는 공식 "기본 폼(is_default)" 데이터로 후보를 좁힌다. 그래도 하나로 안 좁혀지면(사이즈 케이스와 동일하게) 전부 보여준다.

## 이름 매칭 규칙 (2026-09-24)

한국어 이름 → species_id는 `findSpeciesIdByKoreanName`이 PokeAPI 이름 CSV에서 찾는다. 원래는 한 글자도 안 틀리고 똑같아야 찾았는데, 아래 두 가지를 보정했다(`.js`/`.html` 동일).

1. **정규화 비교** (`normalizeKoreanName`): 콜론(`:`, `：`)과 공백을 빼고 영문을 대문자로 맞춘 뒤 비교한다. 한국어 이름 중 한글 말고 다른 문자가 들어간 건 `폴리곤2`, `폴리곤Z`, `타입:널` 세 개뿐이고, 정규화한 뒤 서로 겹치는 이름은 없다(2026-09-24 이름 CSV 전수 확인).
2. **암수 처리**: PokeAPI 한국어 이름에서 암수 기호가 붙은 건 `니드런♀`(29)/`니드런♂`(32)뿐이라, `니드런`만 치면 원래는 "못 찾음"이 떴다(2026-09-24 사용자 신고: `니드런 1 11 11 486`).
   - `findSpeciesIdByGenderAlias`: 이름 끝의 `암/암컷` → `♀`, `수/수컷` → `♂`로 바꿔서 찾는다. **원래 이름으로 못 찾았을 때만** 적용한다. `루가루암`처럼 원래 "암"으로 끝나는 이름이 있어서다. 이름+접미사가 다른 실제 이름과 겹치는 경우는 없다(전수 확인).
   - `findGenderCandidates` + `canReachExactCP`: 암수 표시 없이 치면 두 후보의 종족값으로 "입력한 IV로 그 CP가 정확히 나오는 레벨이 있는지"를 본다. 하나만 맞으면 그쪽으로 계산하고 결과 맨 위에 안내 한 줄을 붙인다. 둘 다 맞거나(예: IV 1/11/11 CP 398) 둘 다 안 맞으면(오타 등) 암수를 붙여서 다시 입력하라고 안내한다. 임의로 한쪽을 고르지 않는 건 암수 종족값이 달라서 틀린 등수가 나오기 때문이다.
   - 카톡 봇은 캐시가 없어서, 이 자동 판별 경로에서는 gamemaster.json(약 1.7MB)을 후보 수만큼(2번) 받는다. 니드런을 암수 없이 칠 때만 타는 경로라 그대로 뒀다. HTML은 `_cache` 덕분에 한 번만 받는다.
   - 안내 문구(존댓말, 기존 봇 말투): `※ '니드런'은 암수가 따로 있어서, CP 486에 맞는 니드런♀로 계산했습니다.` / `'니드런'은 암수가 따로 있습니다. 니드런암 또는 니드런수로 입력해주세요. (예: ?포켓몬 니드런암 1 11 11 486)`(HTML은 예시에 `?포켓몬` 없음). 조사 "은"은 니드런 전용이라 고정했다.

## 알려진 한계 (일부러 안 고친 것)

- **진단 명령(`?포켓몬디버그`, HTML "진단 보기")은 암수 보정을 안 탄다.** `니드런`/`니드런암`을 진단하면 "못 찾음"으로 나온다. 정규화(콜론·대소문자)는 적용된다. 진단은 원인 확인용이라 그대로 뒀다.
- **냐오닉스·에스퍼로·대쓰여너·퍼퓨돈**: gamemaster.json에 암수 폼이 따로 있다(`meowstic`/`meowstic_female` 등). 한국어 이름은 하나라서 니드런 문제와는 다른 경우인데, 결과에 두 폼이 제대로 나오는지는 아직 확인 안 했다(2026-09-24).

- **지가르데 퍼펙트폼(zygarde_complete)**: 메가/프라이멀과 똑같은 문제(dex는 같은데 독립 seed로 취급됨)를 겪지만, gamemaster.json에 `"mega"` 태그가 없어서 자동 판별이 안 된다. 지가르데의 실제 게임 내 변신 메커니즘이 확실치 않아서(잘못 하드코딩하면 오히려 더 틀릴 위험) 이번엔 그대로 뒀다. 조회하면 3가지 폼(10%/50%/퍼펙트)이 다 나오는데, 퍼펙트폼 줄의 레벨/CP/등수는 신뢰하지 말 것.
- **네크로즈마 울트라(necrozma_ultra), 에터나투스 이터나맥스(eternatus_eternamax)**: 같은 유형의 문제가 있지만 둘 다 `released: false`(포켓몬GO에 아직 정식 출시 안 됨)라서 실제로 조회될 일이 없다. 나중에 정식 출시되면 위 지가르데와 같은 문제가 생길 수 있으니, 그때 다시 확인할 것.
- **실바리(silvally)**: 타입:널(type_null)이 진화 대상으로 가리키는데 정작 실바리 자신은 `family` 정보가 없다(위 3번 항목과 같은 유형). `released: false`라 지금은 조회될 일이 없지만, 정식 출시되면 새로 추가한 보정 패스가 자동으로 잡아줄 것으로 예상됨(그래도 실제 출시 후 한 번은 확인할 것).
- **미니오르(minior_core/meteor)의 진짜 기본폼**: PokeAPI 기준 기본폼은 색깔별 유성폼(예: `minior-red-meteor`)인데 PvPoke는 색깔 구분 없이 `minior_meteor` 하나로 묶어놔서, 자동판별 로직이 하나로 못 좁히고 core/meteor 둘 다 보여준다(오답은 아님, 그냥 둘 다 보여줌).
- **체리코(cherrim)의 태양폼**: PvPoke는 "cherrim_sunny", PokeAPI는 "cherrim-sunshine"으로 이름이 달라서 자동판별이 안 먹히고 overcast/sunny 둘 다 보여준다.

이 셋(미니오르/체리코/지가르데류)은 전부 "정확한 답을 하나로 못 좁히면 후보를 전부 보여준다"는 이 프로젝트의 기본 설계 원칙을 그대로 따르는 것이라, 사용자가 결과를 보고 직접 판단하면 된다. 틀린 값을 보여주는 게 아니라 "여러 개 다 보여줌"이므로 심각한 버그는 아니라고 판단했다.

## 다시 열었을 때 확인할 것

1. `raw.githubusercontent.com`의 5개 URL(위 표)이 여전히 살아있는지.
2. `.js`와 `.html`이 여전히 로직상 동일한지 (한쪽만 고치고 다른 쪽을 깜빡하기 쉬움).
3. 메신저봇R에서 실제로 카톡방에 붙여서 테스트할 때는 `ALLOWED_ROOMS`가 원하는 방으로 설정돼 있는지.
   - **배포 방식이 둘이 다르다.** PWA(`.html`)는 `git push`만 하면 GitHub Pages에 자동으로 반영되지만, 카톡 봇(`.js`)은 푸시해도 안 바뀐다. 폰의 메신저봇R에 새 `포켓몬랭크봇.js`를 넣고 컴파일해야 한다. 그래서 코드를 고친 뒤에는 폰에 있는 봇 버전이 저장소 최신과 같은지 꼭 확인할 것.
4. 이 프로젝트 폴더는 `questline-dev/pokemon-rank-bot` GitHub 저장소(조직 계정)와 git으로 연결돼 있다. Public이고 GitHub Pages로 배포 중이다(PWA 설치 테스트를 위해 2026-09-14에 전환). 커밋/푸시는 사용자가 명시적으로 요청할 때만 할 것.
5. **git 커밋 신원**: `user.name`="questline-dev", `user.email`="questline-dev@users.noreply.github.com"으로 로컬 저장소 한정 설정해뒀다. 어느 GitHub 계정에도 연결되지 않는 주소라 커밋으로는 개인 계정을 알아낼 수 없다. 개인 계정의 noreply 주소(`숫자ID+아이디@users.noreply.github.com`)는 숫자 ID만으로 공개 API에서 계정이 조회되니 쓰지 말 것. 전역(`--global`) 설정은 개인 이메일이라 이 리포 안 설정을 지우거나 바꾸지 말 것.
6. 원래 이 저장소는 개인 계정 소유였다. 커밋 히스토리에 실제 이메일이 노출돼 있어서 2026-09-14에 **히스토리를 통째로 버리고** `questline-dev` 조직 밑에 새로 만들었다. 예전 개인 계정 저장소는 2026-09-24 기준으로 공개 조회가 안 된다.
7. **2026-09-24 히스토리 재작성**: 예전 커밋 신원(개인 계정 noreply 주소)과 이 문서 속 개인 계정 표기 때문에 공개 저장소에서 개인 아이디가 보였다. 커밋 10개의 작성자 이메일·메시지·문서를 전부 다시 써서 강제 푸시했다. 그래서 그 전 커밋 번호는 더 이상 쓰지 않는다. 원래 기록은 저장소 밖 로컬 백업(`포켓몬랭크봇-백업-20260924.bundle`)에만 있고, **이 백업은 절대 GitHub에 올리지 말 것**. 푸시한 계정은 GitHub 공개 활동 기록과 PR 작성자에 남으니, 완전히 숨기려면 조직 전용 계정이 필요하다.
8. 코드가 전부 공개돼 있으니, 앞으로 API 키 등 민감정보를 절대 이 저장소에 커밋하지 말 것. 배포 주소는 README.md 참고.

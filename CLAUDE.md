# CLAUDE.md — 이 저장소 작업 규칙

포켓몬GO PvP IV 랭크체커(카톡 봇 `.js` + PWA `.html`).
사용법은 README.md, 설계·한계·점검 목록은 유지보수노트.md, 시행착오는 프롬프트가이드.md에 있다.
여기엔 규칙만 적는다. 자세한 설명은 그 문서들을 본다.

## 1. 소통
- 사용자와는 한국어 반말로, 따뜻하고 명확하게 말한다.
- 답은 결론부터. 근거와 세부는 그 뒤에.
- 커밋 메시지도 한국어로 쓴다.

## 2. 작업 순서
- 조사 → 계획을 채팅으로 제시 → 사용자 승인 → 구현.
- 승인 전에는 코드를 고치지 않는다.
- 요구가 애매하면 추측하지 말고 먼저 묻는다.
  (프롬프트가이드.md의 "애매하면 알아서 판단해 구현"은 예전 방식이고, 이 규칙이 우선한다.)

## 3. 버그 신고
- 사용자가 준 정확한 입력값으로 먼저 재현한다.
- 원인을 찾으면 같은 이유로 틀리는 다른 포켓몬이 있는지 라이브 데이터로 전수 확인한다.
- 스크린샷 같은 실측 증거가 있으면 그 값에 맞춘다. 사례는 프롬프트가이드.md 참고.

## 4. 봇 안내 문구
- 새로 만들거나 바뀌는 문구는 코드에 넣기 전에 채팅에서 먼저 보여주고 확인받는다.
- 봇 말투는 기존 문구처럼 존댓말이다(반말은 사용자와의 대화에만).
  기존 문구 예시는 유지보수노트.md "이름 매칭 규칙" 참고.

## 5. 코드
- `포켓몬랭크봇.js`와 `포켓몬랭크봇.html`은 항상 같이 고치고 계산 로직을 똑같이 유지한다.
  차이는 네트워크 호출(Jsoup ↔ fetch)과 async/await뿐이다. 자세한 건 유지보수노트.md 참고.
- `.js`는 메신저봇R(Rhino) 때문에 ES5 문법만 쓴다.
  `var`/`function`만 쓰고 `let`/`const`, 화살표 함수, 템플릿 리터럴, `for...of`는 금지.

## 6. 배포
- `.html`은 main에 들어가면 GitHub Pages로 바로 배포된다.
- `.js`는 푸시해도 폰 봇에 반영되지 않는다. `.js`를 바꿨으면 사용자에게 꼭 알린다:
  "폰의 메신저봇R에 새 파일을 넣고 컴파일해야 한다."

## 7. git
- 커밋과 푸시는 사용자가 명시적으로 요청할 때만 한다.

## 8. 공개 저장소
- API 키, 비밀번호, 실명, 개인 이메일, 개인 계정 아이디는 절대 커밋하지 않는다.
  코드·문서·커밋 메시지 모두 해당한다.
- '가렸다'고 생각한 값도 공개 API로 거꾸로 찾아지는지 확인한다.
- 저장소 밖의 로컬 백업 번들은 절대 올리지 않는다. 자세한 건 유지보수노트.md 참고.

## 9. 커밋 신원
- 커밋 작성자는 항상 `questline-dev <questline-dev@users.noreply.github.com>`이다.
- 커밋 전에 이 저장소의 git 설정을 확인한다. 다르면 이 저장소에만 설정한다(`--global` 금지).
  ```
  git config user.name "questline-dev"
  git config user.email "questline-dev@users.noreply.github.com"
  ```
- 푸시 전에 새 커밋들의 작성자와 커미터가 모두 이 주소인지 확인한다.
  ```
  git log --format='%an <%ae> | %cn <%ce>' origin/main..HEAD
  ```
  한 줄이라도 다르면 푸시하지 않고 사용자에게 알린다.
- 개인 계정 noreply 주소(`숫자ID+아이디@...`)는 쓰지 않는다. 자세한 건 유지보수노트.md 참고.

## 10. 클라우드 세션
- PR은 열지 않는다(PR 작성자에 개인 계정이 남을 수 있어서).
- 새 브랜치에 푸시까지만 한다. main에 합치거나 main에 직접 푸시하지 않는다.
  main에 합치는 건 사용자 쪽에서 한다.

## 11. 검증
- 유지보수노트.md 표에 있는 외부 데이터 5개(PokeAPI CSV 4개 + PvPoke gamemaster.json)를
  실제로 받아서 확인한다. 기억이나 추측으로 판단하지 않는다.
- "전수조사 했다"는 결과가 있어도 사용자가 반례를 찾으면 반례가 우선이다.

## 12. 문서
- 동작이 바뀌면 README.md, 유지보수노트.md, 프롬프트가이드.md 세 개를 같이 갱신한다.
